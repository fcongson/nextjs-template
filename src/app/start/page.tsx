import styles from './page.module.css';

export default function Start(): React.JSX.Element {
  return (
    <main className={styles.main}>
      <p>
        Get started by editing <code>src/app/start/page.tsx</code>.
      </p>
    </main>
  );
}
